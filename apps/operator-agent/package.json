{
  "name": "@bes/operator-agent",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "diff": "^8.0.2",
    "fastify": "^5.6.1"
  }
}
